{
  "name": "@polylab/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "POLYLAB_DATA_DIR=../../.polylab bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test src"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.0",
    "@polylab/types": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "elysia": "^1.4.16"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "typescript": "^5.9.3"
  }
}
